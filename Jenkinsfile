pipeline {
    agent any

    stages {
        stage('Checkout Success') {
            steps {
                echo 'Code pulled from GitHub'
                sh 'pwd'
                sh 'ls -la'
            }
        }
    }
}